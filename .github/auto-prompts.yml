enabled: true
max_chain_depth: 2
hooks:
  - name: "roadmap-check"
    trigger: "on_completion"
    prompt: "Lanjutkan sesuai roadmap"
    run_once_per_invocation: true
    requires_confirmation_for_commits: true
